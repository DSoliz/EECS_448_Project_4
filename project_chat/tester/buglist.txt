-Friend List search is not working.
-If a user does not hit the logout button they will still appear as connected.
-User list can become infinitely long on Pre-Chat page increasing the size of the page with each user
-Freind list can become infinitely long on Pre-Chat page increasing the size of the page with each user
-Chat.php css can get weird graphics if the list is longer than the initial size of the window.
-Friend list filter is not working properly.
-Some browsers cache the content of pages and are available even after the user logs out (Suspect: Safari Browser)
-On some mobile devices the auto scroll method may bug out (Chrome for Android)
